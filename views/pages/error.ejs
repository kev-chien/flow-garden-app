<% include ../partials/header %>
<section class="my-8 font-sans container max-w-xl m-auto flex flex-col md:flex-row sm:items-center">
    <div class="w-full justify-center items-start px-6 py-0 md:py-8 md:px-8 lg:items-start">
        <h1 class="font-normal"><%= message %></h1>
        <h2 class="font-normal"><%= error.status %></h2>
        <pre><%= error.stack %></pre> <!-- TODO: remove error stack -->
    </div>
</section>

<% include ../partials/footer %>